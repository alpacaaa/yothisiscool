<li class="comment typo-m">
  <i class="tributary">
    {{#user-avatar user=comment.author}}{{/user-avatar}}
    {{#user-name user=comment.author}}{{/user-name}}
    said
    <time {{bind-attr datetime="comment.date"}}>
      <a href="#2">{{timeAgo}}</a>
    </time>

    {{#if comment.repo}}
       about
      {{#link-to 'project' comment.repo.slug}}
        {{comment.repo.name}}
      {{/link-to}}
    {{/if}}
  </i>

  {{{comment.body}}}
</li>
