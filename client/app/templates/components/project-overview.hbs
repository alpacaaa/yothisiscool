
<div class="body author typo-l">
  {{#user-avatar user=project.owner}}{{/user-avatar}}
  <p>
    This is the official {{project.name}} repository.
    <i class="tributary">—
      {{#user-name user=project.owner}}{{/user-name}}
      started crafting
      <a href="{{githubUrl}}">this project</a>
      <time datetime="{{project.created_at}}" title="{{moment project.created_at "MMMM DD, YYYY"}}">
        {{timeAgo}}
      </time>
    </i>
  </p>
</div>
