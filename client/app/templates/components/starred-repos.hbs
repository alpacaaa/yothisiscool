{{#if repos}}
  <section class="section-content">
    <h3 class="notation">Your Starred Repositories</h3>

    <ul class="projects">
      {{#each repos as |repo|}}
        <li class="project">
          {{#link-to "project" repo.slug}}
            {{repo.owner}} <span class="path-divider">/</span> {{repo.name}} <span class="amount">{{repo.count}}</span>
          {{/link-to}}
        </li>
      {{/each}}
    </ul>
  </section>
{{/if}}
